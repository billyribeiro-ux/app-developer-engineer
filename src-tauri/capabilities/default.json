{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for CATALYST",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "store:default",
    "shell:default",
    "log:default"
  ]
}
